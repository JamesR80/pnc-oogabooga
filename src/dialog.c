
loadDialog(0, 0, 0, STR("I am Error"));

    // player - 1000+
	// conductor - 200+ 
	// reporter - 300+
	// baron - 400+
	// detective - 500+
	// starlet - 600+
	// valet - 700+
	// professor - 800+

// Conductor Dialog
loadDialog(1201, 201, 0, 
    STR("Hello."));
loadDialog(201, 1202, 0,
    STR("Hello. I hope you are enjoying the \njourney.\nIs there anything I can do for you?"));
loadDialog(1202, 0, D_HAS_MET, 
    STR("No, thankyou."));
loadDialog(202, 0, D_REPEAT, 
    STR("Hmmpf."));

// Reporter Dialog
loadDialog(1301, 301, 0, 
    STR("Hello."));
loadDialog(301, 1302, 0,
    STR("Hello. Have you heard about ghost \naboard the train?"));
loadDialog(1302, 0, D_HAS_MET, 
    STR("No, tell me more."));
loadDialog(302, 0, D_REPEAT, 
    STR("Shhh, it's a secret."));

// Baron Dialog
loadDialog(1401, 401, 0, 
    STR("Hello."));
loadDialog(401, 1402, 0,
    STR("Hello young lady. \nCare to Join me for a drink?"));
loadDialog(1402, 0, D_HAS_MET, 
    STR("Not right now, maybe later."));
loadDialog(402, 0, D_REPEAT, 
    STR("Bahh *mumble mumble*"));

// Detective Dialog
loadDialog(1501, 501, 0, 
    STR("Hello."));
loadDialog(501, 1502, 0,
    STR("Greetings, Miss....?"));
loadDialog(1502, 0, D_HAS_MET, 
    STR("Adaline. Bye."));
loadDialog(502, 0, D_REPEAT, 
    STR("Move along."));

// Starlet Dialog
loadDialog(1601, 601, 0, 
    STR("Hello."));
loadDialog(601, 1602, 0,
    STR("Excuse me?"));
loadDialog(1602, 0, D_HAS_MET, 
    STR("Never mind."));
loadDialog(602, 0, D_REPEAT, 
    STR("Excuse me?"));

// Valet Dialog
loadDialog(1701, 701, 0, 
    STR("Hello."));
loadDialog(701, 1702, 0,
    STR("Afternoon Miss. \nAnything you need?"));
loadDialog(1702, 0, D_HAS_MET, 
    STR("Not right now. Thankyou anyway."));
loadDialog(702, 0, D_REPEAT, 
    STR("Be with you in a jiff."));

// Professor Dialog
loadDialog(1801, 801, 0, 
    STR("Hello. What are you doing?"));
loadDialog(801, 1802, 0,
    STR("Oh me? Scanning for ectoplasm \nof course."));
loadDialog(1802, 0, D_HAS_MET, 
    STR("I'll leave you to it."));
loadDialog(802, 0, D_REPEAT, 
    STR("... AHA! \n... \nNo, no that can't be it ..."));





// loadDialog(101, 201, STR("Hello."));
// loadDialog(101, 201, STR("Hello."));
// loadDialog(101, 201, STR("Hello."));
// loadDialog(101, 201, STR("Hello."));
// loadDialog(101, 201, STR("Hello."));
// loadDialog(101, 201, STR("Hello."));

